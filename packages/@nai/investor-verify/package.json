{
  "name": "@nai/investor-verify",
  "version": "0.1.0",
  "private": true,
  "description": "Investor verification flow for invest.nguyenai.net private data room — identity declaration, verify.iai.one, payment (VN QR / USD wire), receipt matching, 2FA (TOTP + SMS), access grant. Per INVESTOR_ACCESS_POLICY §3 (LOCKED).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'no build — TypeScript source consumed directly'",
    "typecheck": "tsc --noEmit",
    "test": "tsx src/test.ts",
    "lint": "echo 'TODO: eslint for @nai/investor-verify'"
  },
  "keywords": ["investor", "verification", "kyc", "2fa", "totp", "vietqr", "nguyenai"],
  "dependencies": {
    "@nai/audit": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
