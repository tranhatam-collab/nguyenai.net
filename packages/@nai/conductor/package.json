{
  "name": "@nai/conductor",
  "version": "0.1.0",
  "private": true,
  "description": "Agent orchestration for NAI — 9 NAI Agents + state machine (init→plan→execute→verify→done).",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'no build — TypeScript source consumed directly'",
    "test": "tsx src/test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'TODO: eslint for @nai/conductor'"
  },
  "keywords": ["langgraph", "nai", "nguyenai", "agent"],
  "upstream": "https://github.com/langchain-ai/langgraph",
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
