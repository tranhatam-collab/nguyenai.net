{
  "name": "@nai/scout",
  "version": "0.1.0",
  "private": true,
  "description": "Browser automation for NAI — fetch, parse, extract structured data from web pages.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "echo 'no build — TypeScript source consumed directly'",
    "test": "tsx src/test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'TODO'"
  },
  "keywords": ["browser", "scraper", "automation", "nai", "nguyenai"],
  "upstream": "https://github.com/browser-use/browser-use",
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
