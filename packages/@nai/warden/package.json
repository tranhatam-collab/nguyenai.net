{
  "name": "@nai/warden",
  "version": "0.1.0",
  "private": true,
  "description": "Security policy engine for NAI — policy evaluation, rate limiting, and IP allow-listing with CIDR support.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "echo 'no build — TypeScript source consumed directly'",
    "test": "tsx src/test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'TODO: eslint for @nai/warden'"
  },
  "keywords": [
    "security",
    "policy",
    "rate-limit",
    "ip-allowlist",
    "cidr",
    "nai",
    "nguyenai"
  ],
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
