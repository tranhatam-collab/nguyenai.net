{
  "name": "@nai/ai-provider-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed client for AI Provider Gateway (aiagent.iai.one) — single source for all model invocations.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'no build — TypeScript source consumed directly'",
    "test": "tsx src/test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'TODO: eslint for @nai/ai-provider-client'"
  },
  "keywords": ["nai", "nguyenai", "ai-provider", "gateway"],
  "dependencies": {
    "@nai/prism": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0",
    "@cloudflare/workers-types": "^4.20260629.0"
  }
}
