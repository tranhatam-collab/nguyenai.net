{
  "name": "@nai/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && cp -r public/* dist/ 2>/dev/null || true",
    "preview": "astro preview",
    "typecheck": "astro check",
    "lint": "echo 'TODO: eslint for @nai/web'"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "astro": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
